# homebrew-tap

Homebrew tap for `mdmind`.

## Install

```bash
brew tap dudash/tap
brew install mdmind
```

## What It Installs

- `mdm`
- `mdmind`
- bundled example maps under `share/mdmind/examples`
